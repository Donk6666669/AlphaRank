# @package _global_
name: 3w_pair_combine
notes: "Training on 3w pair data"

defaults:
  - override /dataset: 3w_pair_ori
  - override /model: combine_pair_ranker
  - override /criterion: rank

dataset:
  dataset_args:
    train:
      include_screen: false
      max_screen: null
      include_rerank: true
      max_rerank: null
