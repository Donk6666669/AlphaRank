# @package _global_
name: 3w_triplet
notes: "Training on 3w triplet data"

defaults:
  - override /dataset: 3w_triplet
  - override /model: mlp_triplet_ranker
  - override /criterion: rank_bce
  - override /callbacks: [checkpoint_rerank, lr_monitor, rich, summary]

dataset:
  dataset_args:
    train:
      include_screen: false
      max_screen: null
      include_rerank: true
      max_rerank: null
